{
  "name": "@doxynix/siem-server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./client": {
      "types": "./src/client.ts",
      "default": "./dist/client.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "with-doppler": "bun scripts/doppler.ts",
    "dev": "bun with-doppler -- bun run --watch src/index.ts",
    "type-check": "tsc --noEmit",
    "db:generate": "bun with-doppler bunx drizzle-kit generate",
    "db:push": "bun with-doppler bunx drizzle-kit push",
    "db:studio": "bun with-doppler bunx drizzle-kit studio",
    "db:migrate": "bun with-doppler bun scripts/migrate.ts"
  },
  "dependencies": {
    "@axiomhq/js": "1.8.0",
    "@doxynix/siem-shared": "workspace:*",
    "@neondatabase/serverless": "1.1.0",
    "@t3-oss/env-core": "0.13.11",
    "drizzle-orm": "0.45.2",
    "hono": "4.12.25",
    "postgres": "3.4.9",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "0.31.10",
    "typescript": "7.0.2",
    "wrangler": "4.118.0"
  }
}
